# نسخ هذا الملف إلى `.env` وعدّل القيم حسب بيئتك.
# Vite يقرأ المتغيرات المسبوقة بـ VITE_ فقط.

# عنوان الـ API (يُضاف بعده المسارات من src/api/endpoints.js)
VITE_API_BASE_URL=http://localhost:8000/api
